import classNames from './appFooter.module.css';

const AppFooter = () => {
  return (
    <div className={classNames.appFooter}>
      <p className={classNames.footerTitle}>Breakfast with Jenia</p>
    </div>
  );
};

export default AppFooter;
